[project]
name = "cell-counter"
version = "0.1.0"
description = "A command-line tool to count cells from segmentation files, excluding specified regions."
dependencies = [
    "click",
    "numpy",
    "openpyxl",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"